# IAVM 0.1.4 Backoff Profile Plan

**Goal:** 在 `0.1.3` 已建立最小 retry/backoff 之后，为 IAVM 增加可配置的退避曲线，让 backoff 不再只能是固定间隔。

**当前状态:**
- `0.1.3` 已完整交付，poll/wait 已支持基础 retry/backoff
- 当前 backoff 仍是固定值，缺少 multiplier 与 max cap 控制

## 1. 版本目标

`0.1.4` 先完成三件事：

1. **全局 backoff 曲线**
   - 支持 `RetryMultiplier`
   - 支持 `RetryMaxBackoff`

2. **Capability backoff profile**
   - capability config 可覆盖默认 multiplier / max

3. **测试与文档**
   - 增加 backoff 计算与 profile 覆盖回归
   - 更新 ABI / 计划文档

## 2. Milestones

### Milestone 1：Backoff 曲线

**状态:** 已完成。

**已完成内容:**
- retry backoff 已支持 multiplier
- retry backoff 已支持 max cap
- backoff 计算回归已完成

### Milestone 2：Capability backoff profile

**状态:** 已完成。

**已完成内容:**
- capability config 已可覆盖默认 multiplier / max
- promise 恢复路径会沿用该 profile
- capability backoff profile 覆盖回归已完成

### Milestone 3：文档与交付收口

**状态:** 已完成。

**已完成内容:**
- ABI / backoff 曲线语义文档已更新
- `0.1.4` delivery note 已生成
- 当前版本完成度与边界已明确

## 3. 发布前验证

```bash
go test ./iacommon/... ./iavm/... ./ialang/...
```

## 4. 非目标范围

- 随机 jitter
- 更复杂的 backoff 算法族
- backoff 与 circuit breaker 的联动策略

*计划生成时间: 2026-04-23*  
*目标版本: 0.1.4*  
*状态: 已完整交付*

class Counter {
  constructor(start) {
    this.n = start;
  }
  inc() {
    this.n = this.n + 1;
    return this.n;
  }
  value() {
    return this.n;
  }
}

let c = new Counter(2);
print("counter1: " + c.inc());
print("counter2: " + c.inc());
print("counterv: " + c.value());
